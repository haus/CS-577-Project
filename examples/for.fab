record foo {x : integer};
{
	var r := foo { x := 5};
	var i:integer := 0;
	for i:=0 to r.x do {r.x := r.x+1 };
	for i := 1 to 20 by 3 do {
		write("i: ", i)
	};

	for i := 1 to 20 by 3 do {
		write("i: ", i)
		
		[* if i mod 13 = 0 then exit *]
	}
	
[*	func f() -> integer {
		for i := 1 to 20 do {
			return 3		
		};
		return 0
	};

	var a := f();
	write(a) *]
}